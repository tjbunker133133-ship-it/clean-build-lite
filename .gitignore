node_modules/
dist/
.env
.env.*
!.env.example
env/*.local
.vite/
*.log
.DS_Store
Thumbs.db
*.zip
netlify-dist-deploy.zip
.vercel

# Capacitor / Android local build outputs
android/.gradle/
android/app/build/
android/build/
local.properties

scripts/rescue-smoke.cjs

node_modules
.env*.local

# Archive directory - preserved locally, not tracked in git
# (historical files available in archive/, managed outside version control)
archive/tier2-*/
*.zip
