# Projects

| Folder | Product | Status |
|--------|---------|--------|
| [hud-v1](./hud-v1/) | **HUD V.1** — tactical field HUD (map, panels, PWA) | **Active / canonical** |

Application source stays at the **repository root** (`src/`, `package.json`, `vercel.json`) so Vercel and CI paths stay stable. This folder holds product identity, deploy rules, and cleanup checklists only.
